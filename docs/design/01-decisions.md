# 결정 로그 (append-only)

형식: `#NN | YYYY-MM-DD | <결정> | 채택:<A> | 기각:<B>(사유) | 되돌림:<C>`
4요소(채택안 / 기각안 최소 1개 / 기각 사유 / 되돌리는 조건)가 없으면 여기 적지 않고 `02-open-questions.md` 로 반려한다. 기록 후 `00-index.md` 의 `recent decisions` 를 갱신한다.

---

## `#01` [확정] 메타 모니터링 구조 — 3평면

2026-09-01 | 사용자 확정 | 불변 제약 ④를 C평면(스택 밖 최소 파수꾼)에 한해 완화한다
- **채택**: 3평면 구조 — **A평면**(제품: §2 도식 전체) / **B평면**(메타: 우리 에이전트·컬렉터를 우리 MSA에 부착해 A평면으로 유입 = 도그푸딩, 신규 컴포넌트 0개) / **C평면**(스택 밖 최소 파수꾼: "컬렉터 살아있나 / 수집 지연 늘고 있나" 2개만)
- **기각**: ① 순수 도그푸딩만 — 사유: 컬렉터·저장소 전면 장애 시 감지 수단이 함께 죽어 탐지율 0이 되는 순환 의존 ② 메타 감시 전부를 기성 도구로 — 사유: 조건 4의 "에이전트/컬렉터만 붙이는 형식"과 도그푸딩 검증 가치를 모두 잃음
- **되돌림**: C평면 없이 스택 전면 장애를 탐지할 설계가 제시되면 재검토
- 업계 명칭: Grafana 공식 문서의 *meta-monitoring* — https://grafana.com/docs/grafana/latest/alerting/set-up/meta-monitoring/
- ⚠️ 3단계 구현 주의: 흔히 쓰는 `vector(1)` 상수 Watchdog 알림은 저장소가 가득 차 실제 데이터가 사라져도 계속 발화해 **고장난 스택이 무탐지로 남는** 알려진 결함이 있다(kube-prometheus #2537). 하트비트는 **에이전트 → Kafka → 저장 → 질의를 실제로 관통하는 카나리 신선도**로 잡을 것. 부수효과로 A평면 통합 테스트 겸 데모 소재가 된다.

## `#02` [확정] 코어 범위 — 메트릭·로그·트레이스 3신호 전부 수집

2026-09-01 | 사용자 확정 | 메인 모니터링은 3신호를 모두 수집한다 (잠정안이었던 "메트릭 단독 코어"를 기각)
- **채택**: 메트릭 + 로그 + 트레이스 3신호를 에이전트·수집기가 모두 다룬다. 신호별 저장은 `#03`에 따라 기성 백엔드에 위임한다
- **기각**: 메트릭 단독 코어(트레이스는 3개월차 고도화) — 사유: 제품 정의가 "Pinpoint형 통합 관측"이며, `#03`으로 저장·질의를 기성에 넘기면 3신호 수집 자체는 OTel 파이프라인 하나로 감당 가능해 원래의 기각 사유(2개월에 제품 2개)가 소멸했다
- **되돌림**: 2단계 규모 산정에서 3신호 동시 처리가 팀 4명 역량을 넘는 것으로 나오면 신호별 우선순위를 다시 매긴다(그 경우 메트릭 → 트레이스 → 로그 순)
- 파생: §2 도식은 메트릭 기준 6노드다. 3신호로 확장되면 저장·질의 노드가 신호별로 갈라지는지가 3단계 쟁점이 된다 → `Q10`

## `#03` [확정 · 일부 개정됨 → `#09`] 자체 구현 vs 기성 재사용 경계

> 시각화 부분은 `#09` 로 개정되어 **자체 구현**이 되었다. 저장 엔진·질의 엔진은 아래 그대로 유효하다.

2026-09-01 | 사용자 확정 | 데이터 플레인은 자체 구현하고 저장·질의·시각화는 기성을 쓴다
- **채택**: 자체 구현 = 수집기 · 경보 엔진 · 에이전트 / 기성 사용 = 저장 엔진 · 질의 언어 · 시각화
- **기각**: 저장 엔진·질의 엔진까지 자체 구현 — 사유: 수년짜리 제품이다. 2개월에 넣으면 1개월은 인프라 세팅, 2개월째는 에이전트에 소진되어 **경보 기능이 스텁으로 남아 모니터링 제품이 아니게 된다**
- **되돌림**: 사용자 지정 — *"추후에 저장고까지 만들어야 한다면 그때 재검토하고 스킬을 수정한다."* 판단 시점은 2단계 규모 산정 직후 또는 T0+2M 고도화 착수 시점
- 기성을 써도 바운디드 컨텍스트는 유지된다 — 저장 서비스가 스키마·보관정책·다운샘플링·쓰기배칭을 소유한다. 이 경계표는 2단계 완료조건이다.

## `#04` [확정] 팀 규모와 서비스 개수 상한

2026-09-01 | 사용자 확정 | 팀 인원 4명
- **채택**: 서비스 5~6개를 상한으로 두고 3단계 분할을 진행한다. 레포 분리(폴리레포)도 검토 대상에 올린다
- **기각**: §2의 6노드를 그대로 6개 서비스로 1:1 분할하고 추가 확장 — 사유: 인원 4명 기준 1인당 서비스 1.5개를 넘으면 운영·온콜·CI 유지 비용이 개발 시간을 잠식한다
- **되돌림**: 인원이 6명 이상으로 늘면 분할 입도를 재검토한다

## `#05` [확정 · 보관 기간 개정됨 → `#10`] 비기능 요구사항 수치

> 보관 기간에 장기 계층(1시간 · 1년)이 `#10` 으로 추가되었다. 나머지 수치는 아래 그대로 유효하다.

2026-09-01 | 사용자 확정 | 1단계 제안값을 그대로 채택한다
- **채택**: 가용성 = 수집 경로 99.9% / 조회·시각화 99.5% · 지연 = 수집→조회 p95 30초 / 경보 발화→알림 p95 60초 · 보관 = 메트릭 15일(1분 다운샘플 후 90일) / 로그 7일 / 트레이스 3일 · 처리량 = 메트릭 10,000 포인트·로그 2,000 라인·트레이스 500 span (초당)
- **기각**: ① 더 낮춘 안(가용성 99.5% / 지연 60초 / 보관 절반) — 사유: 장애 인지까지 1분을 넘기면 모니터링 도구로서의 효용이 급감한다 ② 더 높인 안(99.95% / 지연 10초) — 사유: 다중화·백프레셔 처리가 필수가 되어 팀 4명 2개월 범위를 넘는다
- **되돌림**: 2단계에서 감시 대상 서비스 수 × 인스턴스 수로 역산한 실제 처리량이 제안값의 2배를 넘으면 처리량과 보관 기간을 재산정한다
- 수집 경로를 조회보다 엄격히 둔 이유: 데이터는 놓치면 복구 불가지만 조회는 지연되어도 사후 복구가 된다.

## `#06` [확정] 경보 대상 범위 — 3신호 전부

2026-09-01 | 사용자 확정 | 자체 경보 엔진이 메트릭·로그·트레이스 3신호 모두를 대상으로 한다 (Q11 종결)
- **채택**: 지표 임계 규칙 + 로그 패턴 규칙 + 트레이스 이상 규칙을 모두 정의·평가·발화할 수 있다
- **기각**: MVP는 메트릭 기반 경보만 — 사유: 3신호를 수집만 하고 경보를 못 걸면 로그·트레이스가 조회 전용으로 남아 ADR `#02`(3신호 수집)의 가치가 반감된다
- **되돌림**: T0+1M 중간 점검에서 **메트릭 규칙 평가조차 미완**이면 로그·트레이스 경보를 T0+2M 이후 고도화로 되돌린다
- **구현 순서 강제(리스크 완화)**: 메트릭 → 로그 → 트레이스 순으로 만들고 **각 단계가 독립 배포 가능**해야 한다. 세 종류를 동시에 붙이지 않는다. 순서를 바꾸려면 별도 ADR이 필요하다.
- 파생: 경보 도메인이 커져 ADR `#04`의 서비스 5~6개 상한에 압박이 생긴다 → `Q10`에서 함께 판단한다.

## `#07` [확정 · 개정됨 → `#14`] OpenTelemetry 채택 경계

2026-09-01 | OTel을 어디까지 쓰는가 = ADR `#03` "자체구현" 의 실체를 결정한다
- **채택(잠정)**: **B안 — OTel Java Agent 골격 + 자체 Extension**, 수집기(S2)는 우리가 구현하고 OTLP를 수신한다. 측정 항목 이름은 OTel Semantic Conventions를 따르고 발명하지 않는다
- **기각**: ① A안(OTLP 형식만 차용, 에이전트 밑바닥 구현) — 사유: 바이트코드 계측을 처음부터 만드는 것은 2개월 범위를 넘고, 그 시간이 경보 엔진에서 빠진다 ② **C안(OTel Collector까지 채택)** — 사유: S2 Collector가 YAML 설정 작업으로 축소되어 자체 구현이 경보 엔진 하나만 남는다. ADR `#03` 붕괴
- **되돌림**: Extension SPI로 필요한 계측(파이프라인 자기 지표·카나리 발행)을 넣지 못하는 것이 확인되면 A안을 재검토한다
- 근거: Agent는 `-javaagent` 부착이라 제약 ④(비침습)를 그대로 만족한다. Extension은 `-Dotel.javaagent.extensions=` 로 주입하며 `AutoConfigurationCustomizerProvider` SPI로 커스텀 SpanProcessor·Sampler·Exporter를 등록한다(에이전트 재빌드 불필요). 우리 수집기가 OTLP를 받으므로 표준 호환과 자체 구현이 동시에 성립한다.
- **측정 항목 2분류(3단계·4단계 입력)**: ⓐ 대상 앱 = 황금 신호 4축(트래픽·오류·지연·포화도) + JVM·시스템·K8s·DB, Semantic Conventions 명칭 사용 ⓑ **우리 파이프라인 자기 지표** = 에이전트 드롭 건수·버퍼 점유율, 수집기 거부 건수(사유별), **Kafka 컨슈머 랙**, 적재 쓰기 실패율, 규칙 평가 지연·누락, 알림 전송 성공률, E2E 카나리 신선도. ⓑ가 B평면의 실제 내용물이며 이력서 규칙 R3의 실측 근거가 나오는 곳이다.

## `#08` [확정] 감시 대상의 1차/2차 표기 정정

2026-09-02 | 사용자 지적 반영 | `10-requirements.md` 의 "감시 대상 1차 = 우리 자신 / 2차 = 샘플 앱" 표기를 폐기한다
- **채택**: 축을 둘로 분리해 표기한다 — **제품 본연**(쿠버네티스 위 MSA 전반, 검증은 샘플 앱) / **자기 자신**(우리 스택, B·C평면). 그리고 **시간 순서는 샘플 앱이 먼저**임을 명시한다
- **기각**: 기존 "1차 = 우리 자신 / 2차 = 샘플 앱" 표기 — 사유: ① 프로젝트 초기에는 우리 서비스가 존재하지 않아 감시할 대상이 없다. 시간 순서상 샘플 앱이 반드시 선행한다 ② "1차/2차"가 시간 순서인지 중요도인지 구분되지 않아 두 축이 한 표기에 섞였다
- **되돌림**: 팀이 샘플 앱 없이 우리 서비스만으로 개발·검증하기로 하면 재검토한다
- 사용자 지적이 옳았다. 도그푸딩은 제품의 차별점이지 주 용도가 아니며, 개발 순서상으로도 뒤에 온다.

## `#09` [확정] 시각화를 자체 구현으로 변경 (`#03` 부분 개정)

2026-09-02 | 사용자 확정 | `#03` 의 "기성 사용" 목록에서 **시각화만 자체 구현으로 옮긴다**
- **채택**: 시각화(FE)를 자체 구현한다 — 서비스별 상태 대시보드 · 트레이스 타임라인 뷰 · 로그 검색 화면 · 경보 규칙 관리 화면. 저장 엔진과 질의 엔진은 `#03` 대로 기성 유지, 질의 파사드(S3)는 자체 유지
- **기각**: 시각화도 기성(Grafana 임베드) — 사유: ① 팀 노션에 `FE` 가 이미 독립 서비스로 잡혀 있고 기능 4건(`FN-28`~`FN-31`)이 등록되어 있어 `#03` 과 정면 모순이었다 ② `FN-31 경보 규칙 관리 화면` 은 **우리가 자체 구현하는 경보 엔진의 규칙**을 다루므로 Grafana로 대체 불가하다 ③ 화면 구현은 차트 라이브러리 사용 시 2~3주 규모로, 저장·질의와 달리 2개월 일정을 위협하지 않는다
- **되돌림**: T0+1M 중간 점검에서 FE 진척이 **대시보드 1종조차 미완**이면 트레이스 타임라인·로그 검색 화면을 Grafana 임베드로 되돌린다. 단 경보 규칙 관리 화면은 대체 불가하므로 자체 유지한다
- 결과적으로 "우리가 만드는 것" = 에이전트 · 수집기 · 경보 엔진 · 질의 파사드 · 화면 5종이 되어 팀의 5서비스 구성(S1~S5 + FE)과 일치한다.
- 저장과 질의 엔진을 함께 묶어 둔 이유: 질의 엔진은 저장 포맷·인덱스에 의존하므로 저장을 기성으로 쓰는 한 분리해서 자체 구현할 수 없다.

## `#10` [확정] 보관 기간을 3계층으로 확장 (`#05` 부분 개정)

2026-09-03 | 사용자 확정 | 메트릭 보관에 장기 계층을 추가한다
- **채택**: 원본(15초) **15일** → 1분 단위 **90일** → **1시간 단위 1년**. 로그 7일 · 트레이스 3일은 `#05` 그대로
- **기각**: 2계층 유지(1분 90일에서 끝) — 사유: 1시간 단위는 원본 대비 **1/240 용량**이다. 시계열 15만 개 기준 1년치가 약 13억 포인트 = 압축 시 **수 GB 수준**으로 비용 증가가 무시할 만한데, "작년 이맘때 대비" "분기별 추세" 같은 회고가 가능해진다. 비용 대비 효용이 명백히 유리하다
- **되돌림**: 2단계 규모 산정에서 1시간 롤업 생성 작업이 수집 파이프라인에 부담을 준다고 나오면 장기 계층을 뺀다
- 출처: 『대규모 시스템 설계 기초 2』 메트릭 모니터링 장의 보관 정책(원본 1주 → 1분 30일 → 1시간 1년)을 참고했으나, 우리 원본·중간 계층 수치는 `#05` 를 유지했다.

## `#11` [확정] 사내 플랫폼으로 만든다 (SaaS 아님)

2026-09-04 | 사용자 요청으로 명시화 | 모니모니터링은 **사내 플랫폼**(Pinpoint형)이지 SaaS(Datadog형)가 아니다
- **채택**: 사내 플랫폼. 사용자는 우리 회사 개발자·운영자이며, 팀 구분이 필요하면 **쿠버네티스 네임스페이스를 조회 필터 축**으로 두는 선에서 해결한다
- **기각**: SaaS 제품 — 사유: 테넌트 데이터 격리 · 계정/조직 관리 · 과금 · 사용량 제한 · 온보딩 흐름 · SLA가 **필수로 딸려온다**. 팀 4명·2개월에서 이 부가 기능이 수집·경보 파이프라인 개발 시간을 잠식해 정작 모니터링 본체가 미완으로 남는다
- **되돌림**: 프로젝트를 외부 사용자에게 제공할 계획이 생기면 네임스페이스 축을 테넌트로 승격해 재검토한다
- 사실 이미 사실상 선택된 상태였다 — "범위 밖"에 멀티 테넌시·과금·RBAC 세분화가 들어 있었고, 그게 곧 사내형 선언이다. 명시만 안 되어 있었다.
- 참고: Naver Pinpoint도 사내 플랫폼으로 시작했다. 네임스페이스 필터는 멀티 테넌시의 약 1/10 비용으로 "팀별로 나눠 보기" 효용 대부분을 얻는다.

## `#12` [확정] 설계 문서를 두 페이지로 분리

2026-09-04 | 사용자 요청 | 노션 「기능 명세서」가 설계 범위와 구현 목록을 한 페이지에 섞고 있어 분리한다
- **채택**: **「서비스 설계 범위 설정」**(신규) = 서비스 정의 · 사내/SaaS 결정 · 사용 시나리오 · 3평면 · 설계 범위 · 개략 설계안 · 기능/비기능 요구사항 / **「기능 명세서」** = 구현 항목 데이터베이스만
- **기각**: 한 페이지 유지 — 사유: 팀원이 "어떤 모니터링을 만드는지 감이 없다"는 문제가 실재하는데, 34건짜리 구현 목록이 같은 페이지에 있으면 **"무엇을 만드나"가 "무엇을 개발하나"에 묻힌다**. 두 질문은 독자도 갱신 주기도 다르다
- **되돌림**: 팀이 두 페이지를 오가는 것을 불편해하면 설계 범위 문서에 기능 DB를 링크드 뷰로 임베드해 한 화면에서 보이게 한다
- 신규 페이지: https://app.notion.com/p/3d1d7d6851ff816eabf4fc0d1f2c1f90

---
## 2026-09-04 · 신규 계획(쇼핑몰 MSA + APM + Watchdog) 반영 — 전부 `[TENTATIVE]`, 사용자 확인 후 확정

## `#13` [확정 · Q14 종결 → `#38`] 3평면 재정의 (`#01` 개정)

2026-09-04 | 팀원이 3평면을 이해하지 못하는 문제의 원인이 표기에 있음
- **채택**: **A = 감시 대상**(쇼핑몰 MSA, 만들지 않음) / **B = APM 관제**(만듦) / **C = 파수꾼**(만듦). 도그푸딩은 표에서 내리고 "B를 B 자신에게도 붙인다" 한 줄로 유지
- **기각**: 기존 `#01` 표기(A=파이프라인 본체 / B=도그푸딩 / C=파수꾼) — 사유: **덩어리 축과 적용 축이 한 표에 섞여 있다.** B는 신규 컴포넌트가 0개인데 A와 같은 줄에 놓여 별개 물건으로 읽힌다. 팀원 질문("B의 에이전트·컬렉터가 뭐냐")이 정확히 이 지점에서 나왔다
- **기각2**: 중첩형(A=일반 모니터링 → B=A를 감시하는 APM → C=B를 감시하는 와치독) — 사유: ① 모니터링 제품이 2개가 되어 4명·2개월 범위를 넘는다 ② C가 파이프라인 본체를 감시하지 못한다 ③ 감시 대상 애플리케이션이 그림에서 사라진다
- **되돌림**: 새 표기로도 팀원이 이해하지 못하면 3평면 용어 자체를 폐기하고 "만들 것 / 안 만들 것" 2분법으로 간다

## `#14` [기각 · 개정됨 → `#23` · `#33`으로 골자 복원] 에이전트 자체 구현 폐기 (`#03` `#07` 개정)

2026-09-04 | S1 Agent를 만들지 않고 OpenTelemetry Java Agent를 그대로 쓴다
- **채택**: OTel Java Agent 기성품 사용. 자체 Extension(`#07`)은 **폐기가 아니라 Phase 4 선택 항목으로 강등**
- **기각**: `#07`의 "Agent 골격 + 자체 Extension을 MVP에 포함" — 사유: ① 2~3주가 소요되는데 그 시간이 Processor·Watchdog에서 빠진다 ② **백엔드 JD에 바이트코드 계측 키워드가 거의 없다.** 미완성이면 감점 ③ Extension 없이도 B·C평면이 성립함이 확인됐다 — 카나리는 Watchdog이 HTTP를 직접 쏘면 되고, 파이프라인 자기 지표는 우리 Collector·Processor 코드에서 직접 내보내면 된다
- **되돌림**: Phase 3까지 일정 여유가 생기면 Extension을 Phase 4에 복원한다(SPI 플러그인이라 에이전트 재빌드 불필요, 비용이 작다)
- **빠지는 것 1개**: 에이전트 자체 지표(드롭 건수·버퍼 점유율)는 OTel Agent 내부라 우리가 만들 수 없다. → **앱→Collector 첫 홉의 유실이 관측되지 않는다.** 보완: Collector 수신 건수와 대상 앱 요청 수를 대조하는 간접 지표를 `#18` 카나리와 함께 둔다
- **수치 보존 요구**: 카나리 신선도를 Slack으로만 보내면 "탐지 N분 → 30초"의 before/after 근거가 남지 않는다. **카나리 결과를 ClickHouse에도 적재**해 이력서 규칙 R3·R4의 실측 근거로 쓴다
- `#07`의 "OTel Collector 채택 금지"는 **그대로 유효하다** — 채택하면 S2가 YAML 설정 작업으로 축소되어 자체 구현이 경보 엔진만 남는다. OTLP 수신은 우리가 구현한다.

## `#15` [확정 · 개정됨 → `#27`] 저장소 = ClickHouse 단일 (`#03` 구체화)

2026-09-04 | 3신호를 ClickHouse 한 곳에 저장한다
- **채택**: ClickHouse 단일 저장소. 파티션 키·정렬 키·TTL은 우리가 설계한다(ClickHouse는 TSDB가 아니라 기본값이 안 딸려옴)
- **기각**: 신호별 전용 저장소(VictoriaMetrics + Loki + Tempo) — 사유: **성능이 아니라 운영 개수 때문이다.** 우리 목표는 초당 1만 포인트다. 참고로 VictoriaMetrics는 260만/초를 보고하는데, 이는 **40만 유니크 시계열 기준이며 VM 저자 본인 글이라 벤더 편향을 감안해야 한다.** 그럼에도 자릿수가 두 자리 이상 차이나므로 **어느 선택지도 성능은 병목이 아니고**, 4명이 저장소 3~4종을 동시에 운영하는 것이 실제 병목이다. 이 260만이라는 수치는 VM의 것이지 ClickHouse의 채택 근거가 아니다 — 근거는 '우리 규모가 작다'는 사실 자체다
- **되돌림**: Phase 1b 종료 시점에 **로그 `LIKE '%…%'` 조회 p95가 3초를 넘거나 디스크 사용률이 70%를 넘으면** → 1차로 `tokenbf_v1` 스킵 인덱스를 추가하고, 그래도 미달이면 로그만 분리한다. (부하를 우리가 k6로 정하므로 '가정의 N배' 같은 트리거는 영원히 발화하지 않는다)
- **전제조건**: ClickHouse는 단건 insert에 취약하다. `parts_to_delay_insert`=1,000 초과 시 쓰기 지연, `parts_to_throw_insert`=3,000 초과 시 예외(`too many parts`). 공식 권장은 **최소 1,000행, 이상적으로는 10,000~100,000행씩 초당 약 1회**(임계값 1,000/3,000은 ClickHouse 23.6+ 기준. 이전 버전은 150/300). → **클라이언트에서 묶어 넣는 것이 기본 해법이다.**
- **기각(대안)**: async insert(서버 사이드 버퍼링)로 배칭을 ClickHouse에 맡기기 — 사유: 공식 대안이 맞으나 ① 버퍼가 서버 메모리라 장애 시 유실 구간이 생기고 ② 우리는 어차피 1분 집계가 필요해 Processor가 있어야 한다. **다만 배칭의 유일한 해법은 아니므로 `#16`의 근거는 배칭이 아니라 '수신 지연 격리'다**
- 선례: SigNoz = ClickHouse 단일 저장소. Uptrace = 트레이스·메트릭은 ClickHouse + 메타데이터는 Postgres.
- 정정: "Pinpoint가 메트릭용으로 Kafka→Flink→Pinot을 붙였다"는 서술은 부정확하다. 트레이스=HBase는 맞으나 메트릭은 v2.5부터 **Telegraf→Kafka→Pinot 직행**이며 Flink는 폐기된 구버전 방식이다. 취지("메트릭에 별도 전용 저장소")는 사실.

## `#16` [확정 · Q14 종결 → `#38`] Processor 분리 — `Q10` 종결

2026-09-04 | 저장 적재를 S2 Collector에서 떼어내 별도 Processor로 만든다
- **채택**: Collector(수신·검증·Kafka 발행) / **Processor(Kafka 소비·집계·배치 적재)** 를 별도 서비스로 분리
- **기각**: 적재를 Collector 안에 두기 — 사유: Collector는 **받는 쪽이라 지연에 민감한데**(에이전트가 기다린다), 적재는 `#15`에 따라 수천 건씩 묶어 넣어야 한다. 한 프로세스에 두면 **배칭 대기가 수신 지연으로 번진다**
- **되돌림**: Phase 1a 일정이 위태로우면 한 레포에서 시작하되 프로세스는 분리한 채로 둔다(합치지는 않는다)
- **우리가 코드를 쓰는 서비스는 6개로 유지된다** — S1 Agent 삭제(−1) + Processor 추가(+1) = Collector·Processor·Query·Detection·Notification·FE. `#04`의 5~6개 상한 그대로.
- **단 실제 배포·운영 대상은 13개다** — 우리 6 + 쇼핑몰 4 + Kafka + ClickHouse + Watchdog. `#04`의 '1인당 1.5개' 논거를 배포물 기준으로 적용하면 이미 초과이므로, Phase 1a는 **단일 노드 docker-compose로 묶어 흡수**한다(`#19` 참조).

## `#17` [확정 · Q14 종결 → `#38`] 감시 대상 = 쇼핑몰 MSA 4종 (`#08` 구체화)

2026-09-04 | 막연한 "샘플 애플리케이션"을 구체 도메인으로 확정한다
- **채택**: 게이트웨이 · 주문 · 결제 · 재고 4개 서비스의 더미 쇼핑몰. **제품 기능은 개발하지 않되, 감시 대상이 존재해야 하므로 최소 골격은 우리가 작성한다(7~14 p-day).** `#14`가 OTel **Java** Agent를 택했으므로 **JVM(Kotlin/Spring) 통일이 강제된다.** Phase 1a는 **주문·결제 2개만**, 게이트웨이·재고는 서버맵이 필요해지는 1b에 추가
- **기각**①: 도메인 미정으로 두기 — 사유: 서버맵에 HTTP·DB·캐시·외부 API 노드가 모두 나오려면 최소 구성이 필요한데 쇼핑몰이 이를 만족한다. 미정이면 3단계 스키마 설계를 못 한다
- **기각**②: 기성 오픈소스 데모 재사용(OTel Demo · Online Boutique · sock-shop) — 사유: **셋 다 폴리글랏이라 Java Agent가 일부 서비스에만 붙는다.** 계측 커버리지가 갈라져 서버맵이 끊긴다
- **기각**③: Spring PetClinic Microservices — 사유: JVM 단일이라 언어 문제는 없으나 **결제·재고가 없어 외부 API·캐시 노드가 서버맵에 나오지 않는다**
- **되돌림**: JVM 단일이면서 2단계 이상 호출 + DB·캐시·외부 API를 모두 포함하는 기성 데모가 발견되면 교체한다
- **공수 주의**: 이 7~14 p-day는 총량의 6~9%인데 어느 Phase에도 배정되지 않았던 항목이다. `#19`의 Phase 1a 범위에 반드시 포함시켜야 한다.

## `#18` [확정 · Q14 종결 → `#38` · 세부 조합은 구현 시 재검토] Watchdog 기술 조합

2026-09-04 | Dead Man's Switch와 카나리를 무료 조합으로 구성한다
- **채택**: ① Dead Man's Switch = **Healthchecks.io 무료**(20체크·최소 60초 주기·Slack — 공식 확인) ② 카나리 실행기 = **AWS Lambda + EventBridge Scheduler**(EKS와 완전 독립) ③ 카나리 로직 = **`traceparent` 헤더를 직접 만들어** 주문 API 호출 → N초 대기 → Query API에서 그 trace-id 조회 ④ Watchdog 자신도 Healthchecks.io에 등록(감시자의 감시자 문제의 업계 표준 해법) ⑤ 알림 = 별도 Slack 웹훅
- **기각**: ① Prometheus Pushgateway 조합 — 사유: **TTL이 없어 죽은 잡의 마지막 값을 영구 노출한다**(공식 문서 확인). Watchdog 용도로는 안티패턴 ② blackbox_exporter로 카나리 — 사유: 단일 요청-응답만 되고 **다단계 확인이 불가능**하다 ③ k6로 카나리 — 사유: k6는 부하 테스트 도구다. "N초 대기 후 다른 API 조회"는 Lambda 함수가 자연스럽다. k6는 Phase 1a 에러 주입 데모에 쓴다
- **되돌림**: Healthchecks.io 무료 한도(20체크)를 넘으면 Better Stack(모니터+하트비트 **합쳐** 10개, 해상도 1초는 무료 플랜 포함 여부 **미확인**) 또는 CloudWatch 경보의 "데이터 누락=위반" 옵션으로 옮긴다
- **핵심 근거(공식 스펙 확인)**: W3C `traceparent` 는 `00-<32자리 hex trace-id>-<16자리 hex span-id>-01` 형식이며, 호출자가 직접 만들어 보내면 **OTel 기본 W3C propagator가 그대로 채택하고 기본 ParentBased 샘플러가 sampled=01을 존중해 100% 기록한다.** → 카나리가 "보낸 걸 나중에 찾기"가 아니라 **"내가 정한 ID가 도착했는지 확인"** 으로 단순해진다. trace-id 전부 0은 무효.
- **금지**: 상수 하트비트(`vector(1)`). kube-prometheus #2537 실존 확인 — 저장소가 가득 찬 상황에서 이 방식은 계속 정상을 발화하는 반면 실제 알림들은 조용히 죽는다. 대안은 `present_over_time()`.

## `#19` [확정 · Q14 종결 → `#38`] Phase 완료 기준 = 데모 문장

2026-09-04 | 각 Phase의 완료를 산출물 개수가 아니라 데모 문장 하나로 판정한다
- **채택**: Phase 1a "k6로 에러를 내면 N초 안에 슬랙에 알림이 온다" / 1b "느린 요청을 드래그하면 콜트리에서 병목 구간이 보인다" / 2 "Collector를 kill하면 N초 안에 별도 채널로 경보가 온다"
- **기각**: 산출물 존재 여부로 판정 — 사유: 항목을 몇 개 개발했느냐는 **끝에서 끝까지 동작하는지를 보장하지 않는다.** 6개 서비스가 각각 "완성"이어도 관통이 안 되면 데모가 안 된다
- **되돌림**: 어떤 Phase가 예정의 1.5배를 넘기면 **다음 Phase를 미루지 말고 그 Phase의 데모 문장을 축소한다.** 문장을 줄이되 관통은 유지한다
- **보정1 — Dead Man's Switch를 1a로 당긴다**: 코드 몇 줄이므로 Phase 2가 아니라 1a에 넣는다. 차별점이 일정 지연으로 통째로 날아가는 것을 막는 가장 싼 보험. 카나리는 트레이스가 생기는 1b 직후
- **보정2 — Phase 1a 범위 절단(필수)**: 초안 2주는 산정 30~51 p-day 대 실효 가용 25~30 p-day로 **불가능하다.** 아래 4개를 잘라야 성립한다
  1. **1a에서 EKS를 뺀다** (8~12 p-day 절감). 1a 데모 문장("k6로 에러를 내면 슬랙에 알림")에 쿠버네티스는 한 글자도 필요 없다. **단일 노드 docker-compose로 시작하고 EKS는 1b에서 도입한다.** 제약 ②(K8s 필수)는 프로젝트 전체 기준이지 1a 기준이 아니다
  2. **쇼핑몰을 4개 → 2개(주문·결제)로** (`#17`). 게이트웨이·재고는 서버맵이 필요한 1b
  3. **Kafka 단일 브로커** — Strimzi·다중 브로커 금지
  4. **ClickHouse 단일 노드** — 클러스터 구성 금지

## `#20` [확정 · Q14 종결 → `#38` · 소유자 세분 → `#35` `#36`] PostgreSQL 병행 (`#15` 부분 개정)

2026-09-04 | 같은 부트캠프 Team C(OWLBY) 벤치마크에서 드러난 구멍을 메운다
- **채택**: **신호 데이터 = ClickHouse / 상태·관계 데이터 = PostgreSQL.** 경보 규칙·알림 이력·사용자 설정·집계 결과(Rollup)를 Postgres에 둔다
- **기각**: ClickHouse 단일 유지 — 사유: 규칙·이력·설정은 **수정·삭제가 잦은데 ClickHouse의 UPDATE/DELETE는 파트를 통째로 재작성하는 뮤테이션**이다(`#15` 참조). 규칙을 한 번 고칠 때마다 저장소가 무거운 작업을 한다. `#15`의 "단일" 근거였던 '운영 개수 최소화'는 **신호 저장소 3~4종을 막자는 것**이었지 관계형 DB 1개까지 막자는 게 아니다
- **되돌림**: 2단계 규모 산정에서 규칙·이력 건수가 수천 건 이하로 확정되고 수정 빈도가 하루 수 회 미만이면 ClickHouse의 `ReplacingMergeTree` 로 흡수하는 안을 재검토한다
- 선례: Uptrace(트레이스·메트릭=ClickHouse, 메타데이터=Postgres), OWLBY(이벤트=ClickHouse, Endpoint·Alert·Incident·Rollup=PostgreSQL). 같은 패턴이 두 곳에서 독립적으로 나왔다.

## `#21` [확정 · Q14 종결 → `#38` · ① 폐기됨 → `#30` · ③ 개정됨 → `#34`] OWLBY 벤치마크 반영 4가지

> ① Kafka 토픽 2단(`raw`→`validated`)은 `#30` 으로 폐기되었다. ②③④는 그대로 유효하다.

2026-09-04 | 같은 부트캠프에서 완주한 EDR 플랫폼의 검증된 패턴을 가져온다
- **채택**: ① **Kafka 토픽 2단**(`raw` → `validated`) — Processor가 저장을 끝낸 뒤 `validated` 발행, 뒷단은 저장 확정분만 처리 ② **Rollup Worker** — 대시보드 기본 조회는 미리 집계한 결과, 새로고침 시에만 실시간 집계 ③ **S3에 실패 payload 보관 + 재처리** ④ **mTLS 에이전트 인증**
- **기각**: ① 단일 토픽 유지 — 사유: 경보 엔진이 **아직 저장되지 않은 데이터로 규칙을 평가**하는 사고를 막을 수단이 없다 ② 조회를 항상 실시간 집계로 — 사유: 우리 스스로 "읽기는 평소 한산하다 장애 때 폭증"이라 적어놓고 대응 장치를 두지 않았다. **장애 때 조회가 몰리면 그때 저장소가 느려진다** ③ 실패분 폐기 — 사유: 비기능 요구사항의 "유실 0건"과 정면 충돌한다 ④ 인증 없음 — 사유: 누구나 가짜 데이터를 주입할 수 있으면 관측 데이터의 신뢰성이 0이 된다
- **되돌림**: Phase 1a 일정이 위태로우면 ②Rollup과 ④mTLS를 1b로 미룬다. ①토픽 2단과 ③S3는 나중에 넣으면 스키마·재처리 설계를 다시 해야 하므로 **처음부터 넣는다**
- OWLBY가 **따라하면 안 되는 것 2가지**도 확인됐다 — 에이전트 직접 구현(EDR은 OS 커널을 봐야 해서 기성품이 없지만 APM에는 OTel이 있다, `#14` 유지 근거), 자기 감시를 Grafana Cloud에 위탁(그러면 도그푸딩·파수꾼이 사라진다)
- **차별점 확인**: OWLBY와 우리는 파이프라인·ClickHouse가 동일하다. **다른 것은 도그푸딩(B)·파수꾼(C)·카나리뿐이다.** 파이프라인만으로는 같은 부트캠프 안에서 차별화가 안 되므로 C평면을 Phase 2로 미루면 안 된다.
- **감시 대상 차이의 근거**: OWLBY는 감시 대상을 만들지 않았다(레포에 `agents/windows`·`agents/macos` 만 존재). EDR은 운영체제를 보므로 대상이 이미 존재한다. APM은 애플리케이션을 보므로 **애플리케이션이 없으면 요청 자체가 없다** → `#17`의 쇼핑몰 작성이 필요한 이유가 외부 사례로 확인됐다.

## `#22` [확정 · Q14 종결 → `#38` · 부분 정정됨 → `#28`] 롤업(집계 결과)의 저장 위치

2026-09-04 | 리뷰에서 `#20`(PostgreSQL 병행)과 `#21`(Rollup Worker)이 서로 모순됨이 드러나 정리한다
- **채택**: 롤업은 **ClickHouse 집계 테이블**에 둔다. PostgreSQL은 경보 규칙·알림 이력·사용자 설정만 담는다
- **기각**: 롤업을 PostgreSQL에 적재(OWLBY 방식) — 사유: ① 롤업은 **신호의 파생물**인데 원본과 분리되면 조회 시 Query API가 두 저장소를 합쳐야 한다 ② ClickHouse는 `AggregatingMergeTree` + Materialized View로 롤업을 저장소가 직접 만들어줘 Rollup Worker의 일이 줄어든다 ③ OWLBY가 PG를 고른 맥락은 그들의 대시보드가 Endpoint·Alert 같은 **관계형 데이터와 롤업을 함께 조회**하기 때문이며, 지표 중심인 우리 대시보드는 같이 묶을 이유가 없다
- **되돌림**: 대시보드 조회가 ClickHouse 부하를 유의미하게 올리는 것이 측정되면 롤업만 PostgreSQL로 옮긴다
- 파생 정정: 자체 구현 개수가 **6종 → 8종**이 된다(수집기·적재처리기·집계워커·질의파사드·탐지·알림·화면 + 파수꾼). 기존 "6종" 표기는 Rollup Worker와 파수꾼을 빠뜨린 것이었다.

## `#23` [확정 · 뒤집힘 → `#33`] 에이전트 — 자체 Java Agent 구현 (`#14` 뒤집음)

2026-09-05 | 사용자 확정 | 제품 방향이 "Pinpoint 재현"으로 확정되면서 에이전트가 부착 대상이 아니라 **제품의 일부**가 되었다
- **채택**: 자체 Java Agent를 직접 구현한다. `premain` + **Byte Buddy** 바이트코드 계측, 트레이스 ID 생성과 W3C `traceparent` 전파, **해시 기반 1% 샘플링**을 에이전트에서 수행
- **기각**: ① OTel Java Agent 기성품(`#14`) — 사유: 핵심기능 1번("Agent 및 트레이스 추적 체계 구축")이 `-javaagent` 한 줄 부착 작업으로 축소되어 결과물이 남지 않는다 ② OTel Extension만 자체 구현 — 사유: 자동 계측 로직 자체를 만들지 않으므로 바이트코드 계측 경험이 남지 않는다 ③ eBPF 에이전트 — 사유: 커널 레벨이라 애플리케이션 메서드 단위를 볼 수 없어 **콜스택·스레드 덤프·SQL 캡처·URL 통계 4건이 구현 불가**다
- **되돌림**: Phase 1a 종료 시점에 서블릿·RestTemplate·JDBC 3개 플러그인으로 **2홉 트레이스가 화면에 그려지지 않으면** OTel Java Agent로 즉시 전환한다. 수집기 이후 파이프라인은 그대로 재사용되므로 전환 비용은 에이전트에 한정된다
- 최대 리스크는 **비동기 컨텍스트 전파**다. `#25`에서 A평면을 동기 MVC로 못 박아 이 문제를 구조적으로 회피한다

## `#24` [확정 · 개정됨 → `#29`] Redis 역방향 명령 채널 도입

2026-09-05 | 사용자 확정 | 스레드 덤프·실시간 조회·에이전트 설정 조회는 데이터가 거꾸로 흐르는데 기존 설계에 그 경로가 없었다
- **채택**: Redis pub/sub을 **Web ↔ Collector 역방향 명령 채널**로 도입한다. 용도를 ① 스레드 덤프 ② 실시간 액티브 스레드 수 ③ 에이전트 설정 원격 조회 **3가지로 한정**한다
- **기각**: ① Redis 미도입 — 사유: 수집기가 여러 대일 때 **대상 에이전트가 어느 수집기에 붙었는지 화면이 알 수 없어** 위 3개 기능이 구현 불가다 ② Kafka로 대체 — 사유: 수 초 내 요청-응답 왕복이 필요한데 Kafka는 지연이 크고 응답 상관관계를 직접 구현해야 한다 ③ 캐시 용도로도 함께 사용 — 사유: 용도가 늘면 Redis 장애 시 영향 범위가 조회 경로 전체로 확대된다
- **되돌림**: 수집기를 1대로 고정 운영하기로 하면 Redis를 걷어내고 Web → Collector 직접 호출로 대체한다
- 근거: Pinpoint v3.1.0 소스에서 동일 용도 확인 — `realtime/realtime-web/.../RedisActiveThreadDumpService.java`, `RedisActiveThreadCountWebSocketHandler.java`, `redis/src/main/java/.../channel/redis/`

## `#25` [확정 · 대부분 폐기됨 → `#33`] 지원 범위 한정 — 버전 1조합 · 계측 모듈 7종

2026-09-05 | 사용자 확정(권장안 채택) | 자체 에이전트에서는 지원 조합 하나가 곧 검증 매트릭스 하나다
- **채택**: **Java 17 + Spring Boot 3.x 단일 조합**만 지원. 계측 모듈 **7종** — 1a: 서블릿(내장 Tomcat)·RestTemplate·JDBC(MySQL) / 1b: Logback / 이후: HikariCP·Kafka·Lettuce. **A평면 쇼핑몰은 동기 Spring MVC만** 사용한다
- **기각**: ① Java 8/11/17 × Boot 2.x/3.x 다중 지원 — 사유: 매트릭스가 6배가 되고, Java 8과 17은 바이트코드·모듈 시스템이 달라 **같은 계측 코드가 동작하지 않는다** ② Pinpoint 수준 100+ 모듈 — 사유: 플러그인 1개 = 개발·검증 비용 1개이며 2개월에 불가능하다 ③ WebFlux·Reactor·코루틴 지원 — 사유: 비동기 컨텍스트 전파는 자체 에이전트 구현의 최대 난관이고, 여기서 막히면 프로젝트가 정지한다
- **되돌림**: 1a가 예정보다 2주 이상 일찍 끝나면 HikariCP·Kafka를 앞당긴다. **WebFlux는 어떤 경우에도 범위 밖으로 유지한다**
- 근거: 서블릿·HTTP 클라이언트·JDBC 3종만으로 서버맵·스캐터·콜트리·히트맵·URL 통계가 전부 도출된다. 나머지는 커버리지 확장일 뿐이다

## `#26` [확정] 제품 방향 — Pinpoint형 APM 재현, 핵심기능 5개가 명세 골격

2026-09-05 | 사용자 확정 | 문서가 난잡해진 원인이 제품 정의의 모호함이었다
- **채택**: 제품 목표를 **"Pinpoint형 APM을 우리 손으로 만든다"** 로 명시한다. 기능 명세서의 최상위 뼈대는 **팀 핵심기능 5개**(에이전트·알람·시스템 메트릭·히트맵·config UI)로 하고, 세부 요구는 전부 이 5개 아래로 편입한다
- **기각**: ① 범용 관측 플랫폼(Grafana류 메트릭 중심) — 사유: 요구사항의 스캐터·콜스택·인스펙터·URL 통계가 전부 **APM 고유 화면**이라 메트릭 중심 설계로는 도출되지 않는다 ② 보안 이벤트 플랫폼형 구조 — 사유: 그 구조에는 트레이스 개념 자체가 없어 우리 요구의 절반이 표현되지 않는다
- **되돌림**: 팀이 핵심기능 5개를 재정의하면 명세서 골격도 함께 바꾼다
- 파생: 기존 A·B·C 평면 분류는 **문서 구조에서 내리고** 설계 범위 문서에만 남긴다. 명세서는 핵심기능 축 하나로 통일한다

## `#27` [확정] 신호 저장소 — ClickHouse 단일 (`#15` TENTATIVE 해소)

2026-09-05 | 사용자가 Cassandra·ClickHouse·HBase 트레이드오프를 요청해 비교한 결과를 확정한다
- **채택**: 신호(트레이스·메트릭·로그) 저장소는 **ClickHouse 단일**. 상태(경보 규칙·알림 이력·설정)는 PostgreSQL(`#20`)
- **기각**: ① **HBase**(Pinpoint 방식) — 사유: 집계를 DB가 수행하지 않아 서버맵·URL통계·히트맵 집계를 애플리케이션이 직접 계산해야 하고(Pinpoint가 `ApplicationMapStatistics*` 계열 테이블을 20개 넘게 두는 이유), ZooKeeper·HDFS가 동반되어 컨테이너가 늘어난다. 로컬 실측에서 테이블 22개 생성에만 수십 분이 걸렸다 ② **Cassandra** — 사유: `group by`·백분위 같은 **임의 집계가 불가능**하다. 질의를 먼저 정하고 테이블을 설계하는 모델이라 **화면 하나 추가할 때마다 테이블을 새로 설계**해야 하는데, 2개월간 화면 요구가 계속 바뀔 프로젝트에 부적합하다 ③ ClickHouse + Redis 캐시 — 사유: 실시간 요구는 `#24` 명령 채널로 이미 해결되어 캐시 계층이 중복이다
- **되돌림**: 트레이스 단건 조회(트레이스 ID → 스팬 트리) p95가 3초를 넘으면 스킵 인덱스 튜닝을 먼저 하고, 그래도 넘으면 트레이스만 별도 저장소로 분리한다
- 근거: 우리 요구의 중심(히트맵·URL 통계·롤업)은 전부 **집계 질의**다. Pinpoint조차 v2.5부터 메트릭·인스펙터·URL통계·히트맵을 컬럼형(Pinot)으로 옮겼다 — 단건 조회=HBase / 집계=컬럼형으로 갈라졌고 우리는 집계 쪽이 핵심이다. 저장소 3개(HBase+Pinot+MySQL)를 2개로 줄이는 것 자체가 설계 근거가 된다

## `#28` [확정] 자체 구현 범위 — 8종 → 9종 (`#22` `#23` 파생 정정)

2026-09-05 | `#23`이 에이전트를 자체 구현으로 되돌리면서 `#22`의 "8종" 카운트가 낡았다
- **채택**: 자체 구현 **9종** — **에이전트** · 수집기 · 적재 처리기 · 집계 워커 · 질의 파사드 · 탐지 · 알림 · 화면 · 파수꾼
- **기각**: 8종 유지(`#22` 표기) — 사유: 에이전트가 `#23`으로 자체 구현이 되었는데 카운트에서 빠져 있으면 **설계범위 문서와 기능 명세서가 서로 다른 개수를 주장**한다. 실제로 FN-1~10과 FN-58~60(전체 60건 중 13건)이 에이전트에 배정되어 있다
- **되돌림**: `#23`의 되돌림 조건(1a 종료 시 2홉 트레이스 미구현)이 발동해 OTel 기성품으로 전환하면 다시 8종이 된다
- 이 정정이 필요한 지점: 설계범위 §3-2 범위 확정 목록, `SKILL.md` §3 확정표

## `#29` [확정] Redis 용도 4번째 추가 — 에이전트 설정 원격 변경 (`#24` 개정)

2026-09-05 | 리뷰에서 `#24`(용도 3가지 한정)와 FN-49(설정 원격 변경)의 충돌이 드러났다
- **채택**: Redis 명령 채널 용도를 **4가지**로 한다 — ① 스레드 덤프 ② 실시간 액티브 스레드 ③ 에이전트 설정 **조회** ④ 에이전트 설정 **변경**. ④는 화면 → Redis → 수집기 → 에이전트로 내려가고, 에이전트는 다음 수집 주기에 적용 후 결과를 역순으로 회신한다
- **기각**: ① 3가지 한정 유지 + FN-49 삭제 — 사유: "재배포 없이 샘플링률을 바꾼다"가 핵심기능 5번의 완료 조건이라 기능을 뺄 수 없다 ② 설정 변경만 별도 경로(에이전트가 주기적으로 HTTP 폴링) — 사유: 채널이 둘이 되어 `#24`가 Redis를 도입한 이유(수집기가 여러 대일 때 대상 에이전트를 찾지 못함)가 그대로 재발한다
- **되돌림**: 용도가 5번째로 늘어나려 하면 그때 Redis 장애 시 영향 범위를 재평가한다. **캐시·저장소 용도는 어떤 경우에도 추가하지 않는다**(`#24` 유지)

## `#30` [확정 · S3 재처리 부분 개정됨 → `#34`] Kafka 토픽은 `raw` 하나 — `validated` 폐기 (`#21` ① 개정, Q15 종결)

2026-09-05 | 사용자 확정 | 적재 처리기는 ClickHouse에 저장하고 끝낸다. 저장 완료 신호 토픽을 두지 않는다
- **채택**: Kafka 토픽 **`raw` 단일.** 적재 처리기 = `raw` 컨슈머 → ClickHouse 쓰기 → 오프셋 커밋. 실패분 S3 보관·재처리(`#21` ③)는 유지
- **기각**: `#21` ①의 2단 토픽(`raw` → `validated`) — 사유: **읽는 쪽이 없다.** 원래 소비자였던 탐지기는 팀 규칙 1번(탐지기는 Kafka 컨슈머로 만들지 않는다 — Kafka가 죽으면 경보도 죽음)에 따라 질의 파사드를 주기 조회하고, 집계 워커는 `#22`로 ClickHouse Materialized View가 대체했다. 소비자 없는 토픽은 운영 부담만 남긴다. 또한 "저장 확정"은 ClickHouse에 데이터가 존재한다는 사실 자체가 증명하므로 별도 신호가 불필요하다
- **되돌림**: 3단계 이벤트 설계에서 **"저장이 끝난 뒤에만 반응해야 하는 소비자"** 가 실제로 생기면(예: 파수꾼의 카나리 도착 확인, S3 재처리 완료 통지) 그때 재도입한다. 이름은 `validated` 가 아니라 **`stored`** 로 한다
- 이름 교훈: `validated` 는 "검증/인증"으로 읽혀 팀 내 오해를 낳았다. 실제 뜻은 "저장됨"이었다. OWLBY 명칭을 그대로 가져온 것이 원인
- 파생: Figma `48:2` 는 이미 이 결정과 일치한다(적재 처리기 → ClickHouse 단방향). 그림을 고칠 필요 없음. `10-requirements.md` FN-10 "Kafka 발행" 설명의 2단 토픽 언급이 있으면 정정 대상

## `#31` [확정 · 존속 조건 → `#33` · 명령 API 주인 → `#36`] 명령 채널 — Redis 폐기, HTTP 팬아웃 채택 (`#24` `#29` 개정)

2026-09-09 | 사용자 확정 | 검토서 Part B(팀원 김재범)의 안 3 채택. 용도 축소(B-0)로 명령 채널에 남은 기능이 스레드 덤프 하나가 되면서 전제가 바뀌었다
- **채택**: 화면 → 명령 API → **수집기 전체에 HTTP 팬아웃**("이 에이전트 있냐?") → 보유 수집기만 유지 스트림으로 에이전트에 전달 → **응답 동기 반환**. K8s 헤드리스 서비스로 수집기 파드 개별 접근. 실시간 조회는 Query API로, 설정 조회는 수집기 HTTP GET으로, 설정 변경 정본은 PG로 이관
- **기각**: ① Redis pub/sub 유지(`#24`) — 사유: publish가 fire-and-forget이라 "에이전트 미연결/수집기 다운/지연"이 전부 타임아웃으로만 보이고, 응답 경로가 미해결. 기능 하나를 위해 인프라 1종을 유지하는 비용도 정당화 안 됨 ② TTL 레지스트리(Redis SET/EX) — 사유: 수집기 소수인 동안은 팬아웃이 더 단순하고 stale 엔트리 리스크가 없음
- **되돌림**: 수집기 **10대 초과** 또는 팬아웃으로 인한 명령 응답 p95 증가가 실측되면 안 2(Redis TTL 레지스트리)로 전환한다. 명령 API 내부만 바뀌고 화면·수집기·에이전트 인터페이스는 유지
- 파생: 수집기가 완전 무상태가 아니게 되는 첫 지점(헤드리스 서비스). Figma v4.1·v5에 반영

## `#32` [확정 · 8종→7종 → `#33`] 집계 워커 폐지 — 자체 구현 9종 → 8종 (`#28` `#22` 부분 개정)

2026-09-09 | 사용자 확정 | 롤업 저장을 ClickHouse로 옮긴(`#22`) 뒤 워커의 상시 업무가 소멸했음이 확인됐다
- **채택**: 집계 워커를 별도 서비스에서 제외한다. 실시간 집계 = **MV(체인) + AggregatingMergeTree 자동 병합**, S3 재처리분의 지연 도착도 MV가 insert 시점에 자동 반영. **재집계(집계 로직 변경·정합성 복구) SQL의 실행 책임은 적재 처리기**가 갖는다 — 적재 처리기의 책임을 "CH 신호·집계 테이블의 정합성"으로 정의하고, 실행은 관리 잡(필요 시 동일 이미지의 K8s Job)으로
- **기각**: ① 워커 유지 — 사유: 상시 일이 없는 서비스는 배포물·하트비트·CI 비용만 남긴다(OWLBY의 PG 롤업 전제에서 온 관성) ② 재계산 별도 서비스 신설 — 사유: 빈도가 거의 0인 관리 작업에 배포물 +1은 `#31`과 같은 기준으로 과함
- **되돌림**: 재집계 작업이 적재 처리기 본선 소비 지연을 유발하는 것이 실측되면(`#16`의 수신 지연 격리와 같은 판단 기준) 워커를 독립 서비스로 부활시킨다
- 참고: S3 DLQ(`#21`③)는 **유지** — 보류안은 "유실 0건" 위반으로 기각. cold 저장 용도는 규모상 불필요, 필요 시 CH S3 티어링 설정으로 해결(아키텍처 무변경)

## `#33` [확정] 에이전트 — OTel Java Agent 기성품 채택, 자체 Java Agent 폐기 (`#23` 뒤집음 · `#14` 골자 복원 · `#25` `#32` 개정)

2026-09-13 | 사용자 확정(팀 결정) | 에이전트 방향 4번째 결정(`#07`→`#14`→`#23`→`#33`). 이번에는 되돌림 조건을 기다리지 않고 착수 전에 뒤집었다
- **채택**: **OpenTelemetry Java Agent 기성품**을 `-javaagent` 로 부착한다. 자동 계측·컨텍스트 전파·OTLP 전송은 OTel 것을 그대로 쓴다. 자체 Extension은 **스레드 덤프 명령 수신 1건에만 한정**한다(SPI 플러그인, 에이전트 재빌드 불필요 — 커스텀 Sampler·자체 계측 모듈은 만들지 않는다). **샘플링은 수집기로 이동**: 에이전트는 100% 전송(`always_on`), 수집기가 trace ID 해시로 1% 선별, 비율의 정본은 PG(`application_configs`)라 **재배포 없이 변경**된다(핵심기능 5 완료조건 유지). 자체 구현 **8종 → 7종** — 수집기·적재 처리기·질의 파사드·탐지·알림·화면·파수꾼
- **기각**: ① 자체 Java Agent 유지(`#23`) — 사유: **기간·리스크** — Byte Buddy 계측 + 컨텍스트 전파를 7~8주 안에 완성할 확신이 없고, `#23`의 되돌림 조건("1a 종료 시 2홉 미구현")을 기다리면 실패 확인 시점이 늦어 수집기 이후 파이프라인의 실데이터 검증이 통째로 밀린다. **인력 배분** — 4명 중 1명이 에이전트에 묶이면 수집기·적재·탐지·화면 인력이 부족하다 ② OTel Agent + 전면 Extension(`#07` B안) — 사유: 커스텀 Sampler는 수집기 샘플링으로 대체되고, Extension 범위가 넓어지면 ①의 리스크가 축소판으로 재발한다 ③ 스레드 덤프 범위 제외 — 사유: 핵심기능 3 완료조건("스레드 덤프를 화면에서 본다")을 어긴다
- **되돌림**: (a) Extension으로 스레드 덤프가 **1b 종료까지** 동작하지 않으면 스레드 덤프를 범위에서 제외하고 `#31` 명령 채널도 함께 폐기한다(남은 용도 0). (b) 수집기 샘플링 때문에 에이전트→수집기 트래픽이 수집기 CPU·네트워크 한계를 넘는 것이 실측되면 Extension 커스텀 Sampler(에이전트 헤드 샘플링)로 전환한다. (c) 자체 에이전트로의 재복귀는 **없다** — 3회 뒤집기 뒤 확정
- **`#25` 개정**: 계측 모듈 7종·1a/1b 순서는 폐기(OTel이 자동 계측). **Java 17 + Spring Boot 3.x 단일**과 **A평면 동기 MVC**는 검증 범위 축소 목적으로 유지. WebFlux "영구 제외"는 "MVP 범위 밖"으로 완화(OTel은 Reactor 전파를 지원하므로 기술 장벽이 아니라 범위 문제)
- **`#14` 재적용 항목**: ⓐ 에이전트 자체 지표(드롭·버퍼)는 관측 불가 → 수집기 수신 건수 vs 앱 요청 수 대조 + 카나리로 보완 ⓑ **OTel Collector 채택 금지 유지** — OTLP 수신은 우리 수집기가 구현한다(자체 구현이 경보 엔진만 남는 것 방지) ⓒ 카나리 결과는 ClickHouse에도 적재
- **`#31` 존속 조건**: 명령 채널의 유일 용도 = Extension 스레드 덤프. 되돌림 (a) 발동 시 동반 폐기
- 파생: 파수꾼 카나리의 자체 `traceparent`는 OTel Agent가 W3C 전파를 준수하므로 그대로 유효. 로그는 OTel Logback appender 계측으로 OTLP 로그 수신(자체 Logback 모듈 불필요). 문서 파급 — `SKILL.md` §3 표(에이전트·전송·지원조합·샘플링·명령채널·자체구현 6행), Notion 기능명세 **FN-1~10·58~60(13건) 재배정**(에이전트 자체 구현 항목 → OTel 설정/Extension/수집기 샘플링으로 흡수 또는 삭제), Pinpoint 대응표 에이전트 행, 설계 범위 §3-2, Figma v4(`235:2`)·v5(`294:2`) 에이전트 박스 라벨, API 명세의 스레드 덤프 API(유지, Q16 그대로), `10-requirements.md` FR-1 구분 "자체"→"기성(+Extension 1건)"

## `#34` [확정] DLQ = Kafka 토픽, S3 = ClickHouse cold 계층 (`#21` ③ · `#30` 부분 개정)

2026-09-14 | 사용자 확정(팀 결정) | 실패분 보관 위치를 S3에서 Kafka로 옮기고, S3의 역할을 "실패분"에서 "오래된 데이터"로 바꾼다
- **채택**: ① 적재 처리기가 CH에 넣지 못한 메시지(파싱 실패·CH 거절·스키마 위반)는 **Kafka `raw.dlq` 토픽**으로 보낸다. 보관 **30일**(raw 7일보다 길게). 재처리 = DLQ를 다시 `raw`로 흘려보내는 **적재 처리기의 관리 잡**(같은 이미지, K8s Job). ② **S3는 cold 계층**: ClickHouse `storage_policy`로 뜨거운 구간(#10의 로컬 TTL — 트레이스 3일·로그 7일·메트릭 원본 15일)은 로컬 디스크, 그 뒤는 **CH가 스스로 S3 볼륨으로 옮긴다**(`TTL … TO VOLUME 's3'`). 우리 코드 0줄, 조회는 같은 SQL(느려질 뿐). cold 구간의 길이는 `Q26`
- **기각**: ① S3 DLQ(`#21` ③·`#30`) — 사유: **재처리 자동화 비용**. S3 객체를 읽어 재주입하는 코드·IAM·라이프사이클을 따로 짜야 하는데, DLQ 토픽은 컨슈머 오프셋만 되감으면 되어 재처리 잡이 Kafka 클라이언트 하나로 끝난다. Kafka 자체 장애 시 DLQ도 함께 죽는다는 우려는, DLQ가 받는 것이 Kafka 장애가 아니라 **독성 메시지**이고 Kafka 장애 탐지는 파수꾼(C평면)의 몫이라 실효성이 없다 ② 적재 처리기가 원본을 S3에도 병렬 적재(아카이브) — 사유: 쓰기 경로가 둘이 되어 정합성 관리 대상이 늘고, 조회 경로(Athena 등)가 CH 밖으로 갈라진다 ③ TTL 만료분을 별도 잡이 S3로 export — 사유: CH 계층 저장이 같은 일을 기본 기능으로 하므로 만들 이유가 없다
- **되돌림**: DLQ 토픽마저 소비 불가한 사고(스키마 파괴·브로커 디스크 초과)로 재처리가 **반복 실패**하면 S3 DLQ를 재검토한다. cold 계층은 S3 볼륨 조회 지연이 화면 요구(p95)를 깨면 cold 구간을 화면 조회 범위 밖으로 뺀다
- **파생**: FR-3 문구 "실패분 S3 재처리" → "실패분 Kafka DLQ". ERD의 CH 테이블 TTL 절이 `DELETE` 단일에서 `TO VOLUME 's3'` + `DELETE` 2단으로 바뀐다. Figma v4.2 S3 박스 "DLQ" → "cold 계층(CH 계층 저장)", 적재 처리기→S3 화살표 → 적재 처리기→Kafka `raw.dlq` + CH→S3

## `#35` [확정] 에이전트 생존 판정 = CH 부재 규칙, 명단 등록 = 적재 처리기 (`#20` `#33` 파생)

2026-09-14 | 사용자 확정 | `#33`으로 OTel 에이전트가 하트비트를 보내지 않게 되어 "누가 에이전트 생존을 PG에 쓰나"가 드러남(erd-prep §3-2 D)
- **채택**: ① **생존 판정은 탐지**가 한다 — 경보 규칙 하나로 "CH에 해당 `agent_id` 데이터가 90초 동안 없음"을 1분 주기로 평가(API 서버에 최근 구간 에이전트 목록 질의) → `agents.status = DOWN` 갱신 + 알림. 같은 서비스의 에이전트가 전부 DOWN이면 **1건으로 그룹핑**(수집기 장애와 구분은 파수꾼 몫). ② **명단 등록은 적재 처리기**가 한다 — CH에 넣다가 처음 보는 `agent_key`가 나오면 PG `agents`에 `INSERT … ON CONFLICT DO NOTHING`(메모리에 본 키 집합 유지, 파드 생성 시 1회). ③ **수집기는 PG에 쓰지 않는다**(샘플링 비율 읽기만). ④ ERD: `agents.last_seen_at` 컬럼 삭제
- **기각**: ① 수집기가 `last_seen_at`을 30초 배치 UPSERT(D-1) — 사유: 가장 바쁜 경로(mTLS·샘플링)에 PG 쓰기 연결이 추가되고, 같은 정보를 CH 부재로 얻을 수 있어 **중복 저장**이다 ② 수집기가 Kafka에 생존 이벤트 발행, 적재 처리기가 PG 기록(D-2) — 사유: 토픽이 하나 늘고, 수집기↔PG 결합은 샘플링 조회로 이미 존재해 회피 효과가 없다 ③ 등록 전용 서비스 — 사유: `#04` 상한(5~6종)을 7종으로 이미 넘은 상태에서 8번째
- **되돌림**: CH 부재 판정의 지연(적재 지연 p95 30초 + 평가 주기 1분)이 실측에서 "에이전트 다운 인지 90초"를 넘기면 D-1(수집기 30초 배치 쓰기)로 복귀

## `#36` [확정 · 규칙 API 주인 개정 → `#39`] API 서버(구 질의 파사드) = 화면의 백엔드 — 설정·로그인·명령 팬아웃 (`#20` `#31` 개정 · Q24 종결)

2026-09-14 | 사용자 확정 | `#31`의 "명령 API"가 어느 서비스인지, 화면이 써야 하는 PG 표(설정·사용자)를 누가 쓰는지가 미정이었음(erd-prep §3-2 E)
- **채택**: ① **API 서버**(개명: 질의 파사드/Query API → API 서버)가 네 가지를 맡는다 — 신호 조회(CH) · **설정 쓰기**(`application_configs` + `version` 증가, 수집기가 읽어 감) · **로그인**(`users`) · **스레드 덤프 명령 팬아웃**(`#31`의 명령 API 주인, 수집기 파드 전체에 HTTP 동기 질의, 결과를 화면에 반환). ② 규칙 편집은 지금처럼 **화면 → 탐지의 규칙 API** 직접. API 서버 → 탐지 화살표 없음. ③ **PG 소유 = 표마다 하나**(제약 ⑤ DDD): 탐지 = 규칙·경보 이벤트 / 알림 = 채널·전송 이력 / API 서버 = 사용자·설정 / 적재 처리기 = 에이전트 명단(`#35`). ④ `thread_dump_requests` 표 **삭제** — 요청 이력을 PG에 남기지 않는다. 덤프 본문의 CH 저장 여부만 `Q16`으로 남긴다
- **기각**: ① 탐지가 PG 문지기가 되어 설정·사용자·명령까지 대신 씀(E-2) — 사유: **제약 ⑤ 위반** — 탐지 책임은 규칙 평가인데 무관한 표를 소유하게 되어 경보와 무관한 변경마다 탐지를 건드린다 ② 화면(BFF)이 PG 직접 쓰기(E-3) — 사유: 프론트 서버에 DB 자격증명이 들어가고 PG 소유자가 넷에서 다섯으로 는다 ③ `thread_dump_requests` 유지 — 사유: 기능 동작에 불필요하고 `#11` 사내 플랫폼이라 감사 요구가 없다
- **되돌림**: API 서버가 조회 폭증(장애 시 spiky)과 설정·명령을 함께 받아 분리가 필요해지면 설정·명령을 "관리 API"로 쪼갠다(그때 `#04` 상한 재검토). 감사 요구가 생기면 요청 이력 표를 복원한다
- **파생**: Figma v4.3 — API 서버 → PG 실선(설정·로그인) 추가, 명령 팬아웃 출발점을 화면에서 API 서버로, PG 박스 소유 문구 4분할. ERD — `users`·`application_configs` 소유 API 서버, `thread_dump_requests` 제거

## `#37` [확정] ERD 미결 6건 일괄 종결 — Q16 · Q19 · Q20 · Q21 · Q23 · Q26

2026-09-14 | 사용자 확정 | ERD 재설계 착수 전 질문지(erd-prep) 회신. 각 항목의 채택안은 `02-open-questions.md`의 잠정안과 같다
- **채택**: **Q16** 스레드 덤프 본문 = CH `thread_dumps`, TTL 3일 · **Q19** 트레이스 파생 집계 3표(`heatmap_1m`·`url_stats_1m`·`server_map_1m`) = 로컬 30일 → S3 90일 → 삭제 · **Q20** 서버맵 간선 = 적재 처리기가 CLIENT 스팬의 `server.address`/`peer.service`를 `applications.name`에 매핑해 `server_map_1m`을 채운다(`spans.parent_service_name` 컬럼은 두지 않음) · **Q21** `application_configs.enabled_modules` 폐기(필요해지면 재론) · **Q23** 수집기의 샘플링 비율 조회 = PG 직접, 30초 캐시, PG 장애 시 마지막 값 유지 · **Q26** S3 cold 계층 = 모든 신호 90일 후 삭제
- **기각**: Q16 저장 안 함 — 사유: "정상일 때 vs 느릴 때 덤프 비교"가 스레드 덤프의 핵심 쓸모라 이력이 있어야 한다 / Q19 로컬 90일 — 사유: `#34` cold 계층이 생겨 로컬 디스크를 90일 쥘 이유가 없다 / Q20 트레이스 조립로 부모 서비스 탐색 — 사유: 스팬 단건 처리가 아니라 MV로 못 만들고 배치가 하나 더 생긴다; `baggage` 전파 — 사유: 쇼핑몰 설정 수정이 필요해 제약 ④(비침습) 훼손 / Q21 수집기 스팬 필터로 재해석 — 사유: MVP에 쓸 화면·요구가 없어 만든 채로 놀게 된다 / Q23 API 서버 경유 — 사유: API 서버 장애가 수집기 샘플링까지 번진다(수집 경로 99.9% vs 조회 99.5%) / Q26 1년 — 사유: 근거 수치 없이 비용만 는다
- **되돌림**: Q20은 서버맵에 잘못 그려진 간선(매핑 실패)이 실측 5%를 넘으면 트레이스 조립 배치를 추가한다. Q21은 계측 종류별 끄기 요구가 실제로 나오면 컬럼을 복원한다. Q26은 cold 조회 수요·비용 실측 후 신호별로 재조정한다

## `#38` [확정] Q14 종결(잠정 ADR 8건 확정) + ERD 미결 3건 — Q17 long · Q18 앱 단위 · Q22 폐기

2026-09-14 | 사용자 확정 | 질문지 회신 2차. `#37`과 함께 ERD 착수를 막던 미결이 0건이 된다
- **채택**: ① **Q14** — 잠정이던 `#13` `#16` `#17` `#18` `#19` `#20` `#21` `#22`를 그대로 확정한다(`#18`의 기술 조합 세부는 구현 시 재검토). 이후 산출물에 `[TENTATIVE]` 표기를 쓰지 않는다 ② **Q17** `metrics_raw` = **long**(1지표 1행). ③ **Q18** 설정은 **앱 단위만**(`agent_config_overrides` 없음). ④ **Q22** `application_configs.log_level` **폐기**, 대신 수집기에 **전역 하한** 환경변수(`MIN_LOG_LEVEL`, 기본 INFO) 하나
- **기각**: Q17 wide(에이전트 1행 컬럼 N개) — 사유: OTel 데이터가 long 모양이라 적재 처리기가 조립해야 하고, 라벨 조합(`http.route` 등)은 컬럼으로 담을 수 없으며, 지표 추가마다 ALTER가 필요하다. 행 수 1/100의 이점은 CH 정렬 키 압축이 상쇄한다 / Q18 파드별 오버라이드 — 사유: 같은 서비스 파드는 같은 코드·트래픽이라 값이 달라야 할 실제 요구가 없고, "이 파드만 자세히"는 스레드 덤프가 이미 담당. 표 1·조회 2단·화면 1이 늘어난다 / Q22 앱별 유지(수집기 수신 필터) — 사유: 핵심기능 5의 완료조건은 샘플링률·임계값이고 로그 등급은 없다. 쇼핑몰 4종은 우리가 만들어 DEBUG를 켤 일이 없다
- **되돌림**: Q17은 "에이전트 현재 스냅샷" 조회가 p95를 못 지키면 스냅샷용 wide 뷰(MV)를 **추가**한다(원본은 long 유지). Q18은 파드별 요구가 나오면 오버라이드 표 추가. Q22는 특정 앱 로그가 예산(2,000줄/초)의 절반을 넘기는 일이 생기면 컬럼 복원

## `#39` [확정] 규칙·채널 API의 주인 = API 서버, 탐지는 규칙을 읽기만 (`#36` ② 개정)

2026-09-14 | 사용자 확정 | API 명세서 착수 전, 팀원(박유경) 아키텍처 제안("API Server ├─ Rule Service → PG, PG ── 규칙 Polling ──→ Detection")과 `#36`("규칙 편집은 화면 → 탐지의 규칙 API")이 상충해 정리
- **채택**: **B안** — 경보 규칙·알림 채널·규칙↔채널 연결의 CRUD API는 **API 서버**가 제공하고 PG `alert_rules`·`alert_channels`·`alert_rule_channels`를 **API 서버가 쓴다**. 탐지는 이 표를 **읽기만**(주기 폴링, 10~30초) 하고 평가 결과 `alert_events`만 쓴다. 화면은 **API 서버 하나만** 부른다. PG 소유 원칙을 한 줄로 고친다: **"사람이 화면에서 편집하는 표는 전부 API 서버(설정·사용자·규칙·채널·연결), 기계가 쓰는 표는 각 서비스(경보 이벤트=탐지, 전송 이력=알림, 에이전트 명단=적재 처리기)"**
- **기각**: A안(`#36` ② — 탐지가 규칙 관리 API 보유) — 사유: 화면이 백엔드 **두 개**(API 서버 + 탐지)를 상대해 인증·에러 형식·CORS를 두 번 구현하고 API 명세도 두 묶음이 된다. 규칙은 수십 개·양식 변경이 드물어 "두 서비스 동시 수정" 비용이 "문 두 개" 비용보다 작다. 팀원 제안과 일치해 합의 비용도 낮다
- **되돌림**: 규칙 양식(metric_kind·조건식) 변경이 잦아져 API 서버 배포가 탐지에 끌려다니는 일이 반복되면 A안으로 복귀한다(표 구조는 그대로, 소유자·엔드포인트만 이동)
- **보충 확인(2026-09-16, 사용자)**: 경보 규칙은 **팀 공용** — 유저별 사본 없음, 누가 수정하든 한 줄이 갱신되어 모두가 같은 규칙을 본다(`#11` 사내 플랫폼과 일관). 유저 연결은 두지 않는다 — `alert_rules`·`alert_channels`에 `updated_by` **추가 안 함**(사용자 결정 2026-09-16, `application_configs.updated_by`는 기존대로 유지). 개인별 규칙은 Phase 4 고도화 후보로만 메모
- **파생**: ERD `alert_rules`·`alert_channels`·`alert_rule_channels` 소유 표기 탐지→API 서버 · 기능명세 FN-24(경보 규칙 CRUD) 담당 API 서버 · 설계 범위 §4-2 03 "규칙 편집 — 탐지의 규칙 관리 API" → "API 서버" · Figma v4.3 화면→탐지 "규칙 관리 API" 화살표 제거, 탐지→PG는 "규칙 읽기 + 이벤트 쓰기"

## `#40` [확정] 4xx 대역 경보 규칙 지원 — `metric_kind` `4XX_RATE` 추가, `service_health_1m`에 `cnt_4xx`·`cnt_5xx` (Q27 종결 · `#37` 보강)

2026-09-14 | 사용자 확정 | FN-25(4xx 비율·5xx 비율·특정 코드 횟수 규칙 유형, Phase 1a)가 ERD `metric_kind`(5XX_RATE만)와 CH `service_health_1m`(cnt·err_cnt·dur_q)으로는 표현되지 않아 API 명세에서 근거를 뺐던 것을 해소
- **채택**: ① PG `alert_rules.metric_kind` 값에 **`4XX_RATE`** 추가(기존 `5XX_RATE`·`P95_LATENCY`·`CPU`·`HEAP`·`GC_TIME`·`AGENT_DOWN` 유지) ② CH `service_health_1m`에 **`cnt_4xx`·`cnt_5xx` AggregateFunction(sum, UInt8)** 컬럼 추가, `mv_service_health_1m`이 SERVER 스팬의 `http_status` 대역(400~499 / 500~599)으로 나눠 `sumState`. 기존 `err_cnt`(status_code=ERROR)는 유지 ③ 탐지 규칙 평가: `4XX_RATE = cnt_4xx/cnt`, `5XX_RATE = cnt_5xx/cnt`(err_cnt가 아니라 대역 컬럼 기준) ④ "특정 코드(500·503) 횟수" 규칙은 **Phase 2**로 미룸(FN-25 Phase 표기 분리)
- **기각**: ① FN-25를 범위에서 제거 — 사유: 팀 요구("실패 에러가 몇 번대인지 나눠 보라")가 명시돼 있고 4xx는 클라이언트 문제·5xx는 우리 문제라 대응이 달라 규칙 유형으로 갈라야 한다 ② `url_stats_1m`에서 탐지가 매분 합산 — 사유: 탐지가 URL×파드 수백 줄을 매분 읽어 60초 p95를 위협(`#30` 근거와 동일) ③ `http_status`별 Map 컬럼으로 모든 코드 세기 — 사유: AggregatingMergeTree에서 Map 합산은 지원이 약하고 1a에 쓸 화면이 없다
- **되돌림**: 4xx 규칙이 실제로 한 건도 등록되지 않은 채 Phase 2가 끝나면 컬럼·enum을 제거한다
- **파생**: ERD `service_health_1m` 컬럼 2개 추가 + `alert_rules.metric_kind` 설명 갱신 · API 명세 `POST /alert-rules` 근거에 FN-25 복원, `metric_kind` 예시에 `4XX_RATE` · 기능 명세 FN-25 설명에 "특정 코드 횟수는 Phase 2" · `references/erd-clickhouse-guide.md`는 초안이라 미갱신(Notion ERD가 정본)

## `#41` [확정] 카나리 경로를 `#18` 원안(쇼핑몰 API 경유)으로 바로잡고, 수집기 샘플링에 카나리 예외를 둔다 (`#33` 파생 보정)

2026-09-17 | 사용자 확정(A안) | 팀원 리뷰 — API 명세 호출 흐름이 파수꾼→수집기 직접 주입으로 그려져 `#18`과 어긋나고, `#33`의 수집기 1% 해시 샘플링이 카나리의 `sampled=01` 100% 보장을 깨뜨린다
- **채택**: ① 카나리 경로 = 파수꾼이 `traceparent`를 직접 만들어 **쇼핑몰 주문 API를 HTTP로 호출** → 쇼핑몰에 부착된 OTel Agent가 기록 → 수집기(gRPC OTLP) → Kafka → 적재 처리기 → CH → API 서버에서 확인 (`#18` 원안 복원 = 문서 오류 정정) ② 카나리 요청에 W3C `tracestate` 카나리 표시(`monimon=canary`)를 붙이고, **수집기는 span `trace_state`에 이 표시가 있으면 1% 해시 샘플링을 건너뛰고 무조건 통과**시킨다
- **기각**: ① 수집기 직접 주입(API 명세 v3 그림) — 사유: 앱→에이전트→수집기 첫 홉(에이전트 미부착·버퍼 드롭·mTLS 인증서 만료)이 카나리 경로에서 빠진다. 이 구간은 기성 OTel Agent라 자체 지표도 없어(`#33` "빠지는 것") 카나리 외 관측 수단이 없다 ② 해시가 보존 구간에 드는 trace ID를 파수꾼이 골라 쓰기(B안) — 사유: 수집기 코드는 무변경이지만 해시 함수·비율 계산식이 바뀌면 카나리 보존이 **알림 없이** 깨지고, 그 증상이 곧 파수꾼 거짓 장애 알람이다 ③ 예외 없이 유지 — 사유: 비율 1%에서 카나리 트레이스의 99%가 버려져 신선도 판정 불가
- **되돌림**: 외부 요청이 카나리 표시로 샘플링을 우회하는 남용이 실측되면(쇼핑몰 외부 공개 시) 표시 검증(서명 값 또는 파수꾼 출발지 제한)을 추가한다. 샘플링이 에이전트 헤드 샘플링으로 돌아가면(`#33` 되돌림 b) 예외 규칙은 ParentBased 샘플러의 `sampled=01` 존중으로 대체하고 제거한다
- **파생**: API 명세 호출 흐름 그림 수정(2026-09-17 반영) · **미반영**: API 명세 gRPC 절 "파수꾼의 카나리 주입도 이 문을 그대로 쓴다" 문장과 샘플링 문장(예외 미기재) · `GET /api/v1/internal/canary/freshness`의 조회 기준 `service_name=canary-probe`(쇼핑몰 스팬은 쇼핑몰 서비스 이름으로 들어오므로 카나리 표시 또는 trace ID 기준으로 변경 필요) · 기능명세 FN-54·FN-55 문구 · Figma 파수꾼 화살표

## `#42` [확정] PostgreSQL 접근 = JPA(Hibernate) — Spring Data JDBC 기각

2026-09-20 | 사용자 확정 | 4단계 기술 스택 미확정 항목(Notion "사용할 라이브러리 정리") 검토 중 PG 접근 방식 결정. 선정서 추천안은 Spring Data JDBC였으나 사용자가 JPA로 뒤집음
- **채택**: **Spring Data JPA(Hibernate)** 로 PG 9표 접근. 단 가드레일 5종을 확정 조건으로 함께 적용 — ① 발송 대기 큐·경보 이벤트 선점은 **네이티브 쿼리 `FOR UPDATE SKIP LOCKED`** (JPA 비관적 락 어노테이션이 아님) ② `spring.jpa.open-in-view=false` 고정 ③ 목록 조회는 `fetch join` 또는 `@EntityGraph` 명시, 지연 로딩 기본값에 기대지 않음 ④ Kotlin 엔티티에 `data class` 금지, Gradle `allOpen`·`noArg` 플러그인 적용(선정서 반대 근거 "Kotlin 궁합" 해소) ⑤ 실행 SQL 로깅 상시 활성화(p6spy 또는 바인딩 파라미터 로깅) — 선정서 반대 근거 "쿼리가 나갔는지 눈에 안 보인다" 해소
- **기각**: ① **Spring Data JDBC** — 사유: 채택 근거였던 "학습 비용 절감"이 성립하지 않는다. **팀원 전원이 JPA 경험 보유**(2026-09-20 사용자 확인)로 JPA의 학습 비용이 0인 반면 JDBC는 신규 학습이 발생하고, 투입 비율 6:2:1:1(`Q28`)에서 그 시간이 크리티컬 패스에서 나간다. PG는 9표·수십 MB·CRUD 중심이라 ORM 추상화 비용이 성능 문제로 번질 규모가 아니다. 선정서가 든 반대 근거 3건은 가드레일로 해소된다 — "잠금 SQL"은 JPA에서도 네이티브 쿼리로 동일(①), "쿼리가 안 보인다"는 SQL 로깅(⑤), "Kotlin 궁합"은 allOpen·noArg(④). "CH는 JDBC인데 PG만 JPA"는 두 DB의 용도(대량 삽입·분석 vs CRUD)가 달라 근거가 되지 않는다 ② MyBatis 등 SQL 매퍼 — 사유: 후보로 검토된 바 없고 팀 경험도 없어 ①보다 학습 비용이 크다
- **되돌림**: N+1 또는 트랜잭션 경계 문제로 **디버깅이 2회 이상 반나절을 넘기면** 해당 리포지터리만 `JdbcTemplate` 네이티브 쿼리로 내린다(전면 전환 아님). 표가 9개뿐이라 부분 전환 비용이 낮다
- **파생**: Notion **"사용할 라이브러리 정리" PG 접근 토글 갱신 완료(2026-09-20)** — 제목 ✅ 확정, 비교표 JPA 1행/JDBC 2행 교체, 판단기준 → 가드레일 5종 + 되돌림 한 줄. 어절 대조 불일치 0. 굵게 표기는 페이지 규칙(채택안만 굵게)에 맞춰 JPA로 이동 · **기술 스택 선정서에는 PG 접근 토글이 없음**(9번 "상태 저장소 — PostgreSQL"은 저장소 선택 비교표라 대상 아님) → 선정서 갱신 불필요 · CH 접근(적재 client-v2 / 조회 JDBC)과는 무관하며 각각 별도 결정

## `#43` [확정] 서버맵 그래프 라이브러리 = React Flow(xyflow) — Cytoscape.js 기각 (선정서 추천안 뒤집음)

2026-09-20 | 사용자 확정 | 4단계 미확정 항목 검토. 선정서 추천안은 `ECharts + Cytoscape`였고 차트(ECharts)는 유지, 그래프만 뒤집음
- **채택**: **React Flow(xyflow)** 로 서버맵(핵심기능 — 호출 관계 그래프) 구현. 자동 배치는 **dagre** 를 붙여 해결(레이아웃 코드 수십 줄). 노드는 React 컴포넌트로 만들어 서비스명·초당 처리량·에러율 표시를 JSX로 직접 구성
- **기각**: ① **Cytoscape.js** — 사유: 강점인 "수천 노드 처리·그래프 연산"이 우리 규모에서 발휘되지 않는다. 서버맵 노드는 **우리가 직접 작성하는 쇼핑몰 MSA 서비스**(`#17` `#25`)라 수십 개 이하로 상한이 정해져 있다. 반대로 실제 요구인 "노드 안에 지표 표시"는 Canvas 렌더라 좌표 계산으로 직접 그려야 한다. 차트를 ECharts로 둔 상태에서 프론트 담당(투입 1)이 익힐 API가 2개가 된다 ② D3 직접 구현 — 사유: 드래그·확대·간선 라우팅을 전부 직접 짜야 해 투입 비율상 불가
- **되돌림**: 감시 대상 노드가 **100개를 넘어** DOM 렌더가 버벅이면 Cytoscape.js로 교체한다. 현재 범위에서는 발생하지 않는다
- **파생**: Notion **"사용할 라이브러리 정리" 반영 완료(2026-09-20)** — §3 프론트 `서버맵 — Cytoscape.js` 토글 → `서버맵 — React Flow (xyflow)` ✅ 확정, §3 한눈에 보기 표, §7 "아직 안 정한 것" 표의 `프론트 차트 · 그래프` 행을 `프론트 차트`로 좁힘. 차트(ECharts)는 **별도 토글이며 🟡 추천안 유지**(미확정) · **선정서에는 해당 토글 없음**(14번 화면 토글은 자체구현 vs Grafana 임베드만 다루고 차트·그래프는 "4단계에서 정한다"고만 기재) · 차트는 **ECharts 추천안 유지**(Canvas·`brush` 내장으로 히트맵 1만 점 드래그 요구 충족, 확정 여부는 미표기) · **미반영**: Notion 선정서·라이브러리 정리

## `#44` [확정] IaC = Terraform 단일 — SAM 기각, AWS 기반 인프라 전체를 코드화 (선정서 추천안 뒤집음)

2026-09-20 | 사용자 확정 | 4단계 미확정 항목 검토. 선정서 추천안은 `Python + SAM`이었고 파수꾼 **언어는 미정 유지**(담당자 Python 경험 확인 필요), IaC만 뒤집음
- **채택**: **Terraform** 으로 AWS 자원 전체를 단일 코드 베이스로 관리 — VPC·VPC 엔드포인트·EKS 클러스터·노드 그룹·RDS·ECR(수명주기 정책 포함)·Secrets Manager·S3, 그리고 **파수꾼 Lambda + EventBridge 스케줄까지 포함**. state는 S3 백엔드 + 잠금(S3 네이티브 잠금 또는 DynamoDB 테이블)
- **기각**: ① **SAM** — 사유: 파수꾼은 Lambda 1개 + 스케줄 1개라 Terraform으로도 수십 줄이다. 이걸 위해 두 번째 IaC 도구를 도입하면 학습·CI 경로가 두 벌이 되는데, 정작 SAM은 VPC·EKS·RDS를 다루지 못해 인프라의 대부분이 코드 밖(콘솔 또는 eksctl)에 남는다 ② 콘솔 수동 생성 — 사유: 예산 상한 월 $150~300(2026-09-20 사용자 확정)에서 야간 노드 축소·재생성을 반복할 계획인데, 설정 기록이 없으면 재현이 불가능하고 실수 복구가 기억에 의존한다
- **되돌림**: Terraform 초기 세팅(state 백엔드·EKS 모듈)이 **착수 첫 주를 넘기면** 접고 `eksctl + SAM` 조합으로 내린다. 인프라 코드화는 목적이 아니라 수단이다
- **파생**: Notion **"사용할 라이브러리 정리" 반영 완료(2026-09-20)** — §4 파수꾼 `언어 · 인프라 정의 — Python 또는 Kotlin / AWS SAM` 토글 → `Python / Terraform` ✅ 확정(언어와 IaC가 한 토글에 합쳐져 있음), §4 한눈에 보기 표, §7 표에서 `파수꾼 언어 · IaC` 행 삭제 · **구조 부채**: IaC 비교표가 §5 `monimo-deploy`(인프라 레포)가 아니라 §4 파수꾼 절에 있다. 본 ADR로 Terraform 범위가 AWS 자원 전체로 넓어졌으므로 §5로 이동하는 것이 맞음 — 미이동 · **선정서에는 IaC 토글 없음**(21번 "아직 안 정한 것"에도 없음) · 파수꾼 **언어(Python vs Kotlin)는 여전히 미정** — 담당자(투입 1②) Python 경험 여부가 판단 기준 · GitHub Actions에서 Terraform 실행 시 **AWS OIDC** 사용(영구 액세스 키 저장 금지) · **미반영**: Notion 선정서·라이브러리 정리

## `#45` [확정] 파수꾼 구현 언어 = Python — Kotlin 기각 (`#44` 미정 항목 종결)

2026-09-20 | 사용자 확정 | `#44`에서 IaC만 정하고 언어는 "담당자 Python 경험 확인 필요"로 남겨 둔 것을 종결
- **채택**: **Python** 으로 파수꾼 Lambda 구현. 배포는 `#44`의 Terraform이 zip 패키지로 올림. 하는 일은 `#41` 경로 — `traceparent`를 직접 만들어 쇼핑몰 주문 API를 HTTP 호출한 뒤, 잠시 후 API 서버의 카나리 신선도를 조회해 판정
- **기각**: ① **Kotlin/JVM** — 사유: 파수꾼 본체는 HTTP 호출 2회 + 타임스탬프 비교로 **30~40줄** 규모인데, JVM은 fat jar 10MB 이상·콜드 스타트 1~3초·메모리 512MB 이상을 요구해 코드 대비 포장이 과하다. 또 `#44`가 Terraform 단일 배포인데 Kotlin이면 Gradle 빌드·shadow jar 단계가 파수꾼 전용으로 하나 더 붙는다. 불변 제약 ②의 "Kotlin 필수"는 자체 구현 7종 중 서비스 6종이 이미 충족한다 ② 컨테이너 이미지 Lambda — 사유: ECR 이미지 빌드·푸시가 배포 경로에 추가되는데 30~40줄 스크립트에 불필요하다
- **되돌림**: 파수꾼 담당자(투입 1②)가 Python 작성에 막혀 **착수 첫 주 안에 관통 확인을 못 하면** Kotlin으로 바꾸고 콜드 스타트는 SnapStart 또는 1분 주기 호출에 의한 웜 유지로 감수한다
- **파생**: Notion **"사용할 라이브러리 정리" 반영 완료(2026-09-20)** — `#44`와 같은 토글(§4 파수꾼)에 함께 기록, §4 한눈에 보기 표 `언어` 행 `Python` ✅ · 파수꾼 레포의 CI는 GitHub Actions + AWS OIDC(`#44`) 경유 Terraform apply

## `#46` [확정] 레포 구성 = 폴리레포 5개 (backend는 멀티모듈) — 모노레포 1개 · 서비스별 9개 · docs 레포 기각 (Q9 종결)

2026-09-21 | 사용자 확정 | Notion「레포별 파일 구성」(3e1d..783b) 채택. GitHub org `2026-techeer-project-team-b` 에 이미 생성된 5개 레포(2026-09-20 생성, 2026-09-21 퍼블릭 전환)를 정식 구성으로 확정
- **채택**: `monimo-backend`(Gradle 멀티모듈: common · collector · ingester · api-server · detector · notifier) · `monimo-web` · `monimo-shop` · `monimo-watchdog` · `monimo-deploy` 5개. 모듈은 `:common` 만 의존. 퍼블릭 + MIT 라이선스. `main` 보호는 **PR 필수만**(리뷰 승인 필수 없음: 팀원 신뢰, 사용자 결정), "CI 통과 필수"는 CI가 생긴 뒤 추가. 설계 문서는 `monimo-backend/docs/design/` 으로 이관 예정(결정 기록 원본 = 레포, Notion = 팀 공유 사본)
- **기각**: ① **모노레포 1개** — 사유: 파수꾼은 우리 스택과 같이 죽으면 안 되므로(`#01` C평면) 레포와 배포 경로까지 분리해야 하고, 쇼핑몰은 감시 대상이라 우리 코드와 배포 주기·버전이 달라야 한다 ② **서비스별 레포(총 9개)** — 사유: `common`(Kafka 메시지 형식) 변경 한 번이 레포 2~5개 동시 수정을 부르고, 4명에 CI·보호 규칙 9벌은 관리 비용만 늘린다 ③ **`monimo-docs` 별도 레포(6개)** — 사유: Notion이 팀 공유본이라 문서 전용 레포는 사본 하나를 더 만들 뿐이다. backend `docs/` 로 충분
- **되돌림**: backend 한 모듈의 CI가 다른 모듈 PR을 반복해서 막거나(paths 필터로 해결 안 됨), 모듈별 배포 주기가 크게 갈려 머지 충돌이 주 2회 이상 나면 **그 모듈만** 별도 레포로 뗀다
- **파생**: Q9 종결 · 첫 커밋 파일(README · LICENSE · .gitignore · PR 템플릿 · CODEOWNERS + 최상위 폴더 `.gitkeep`) 로컬 준비 `~/monimonitoring/repos/` · 개발환경 계획 `.omc/plans/devenv-setup.md` rev 3 의 1단계 완료

## `#47` [확정] 설계 문서 이관 = `monimo-backend/docs/design/` — deploy 레포 · Notion 단독 원본 기각 (`#46` 파생)

2026-09-21 | 사용자 확정 | 로컬 `~/monimonitoring/design/` 을 레포로 옮김. 개발환경 계획 3단계
- **채택**: `monimo-backend/docs/design/` 에 설계 문서 전체(00-index · 01-decisions · 02-open-questions · 10-requirements · canvas · references · web-v2) 이관. **원본 규칙: 결정 기록 원본 = 이 레포, Notion = 팀이 읽기 편한 사본.** 문서 변경도 PR로만. 도구 작업 기록(`.omc/`)과 백업(`*.bak`)은 제외. 원 자리에는 포인터 한 줄만 남김
- **기각**: ① **`monimo-deploy/docs`** — 사유: deploy는 CI 봇이 `image.tag` 한 줄씩 커밋하는 레포라 문서 PR이 봇 커밋 사이에 묻히고, 1a 동안은 아직 비어 있는 레포다. 설계 결정 대부분은 backend 5개 서비스에 관한 것이라 코드 옆이 찾기 쉽다 ② **로컬 유지** — 사유: 팀원 3명이 볼 수 없고 백업이 없다 ③ **Notion 단독 원본** — 사유: 결정 기록은 append-only인데 Notion은 누가 언제 무엇을 바꿨는지 diff가 남지 않고, 한글 쓰기 시 글자 깨짐 사고가 있었다
- **되돌림**: 문서 PR이 backend CI를 반복해서 돌려 방해가 되면 먼저 CI에 `docs/**` paths 제외를 건다. 그래도 문서 PR과 코드 PR의 머지 충돌이 주 2회 이상 나면 문서만 별도 레포로 뗀다
- **파생**: 나머지 4개 레포 README "관련 문서"에 링크 추가

## `#48` [확정] 테스트 프레임워크 = Kotest (기본 스타일 BehaviorSpec) — JUnit 5 기각

2026-09-22 | 사용자 확정 | 개발환경 4단계에서 JUnit 5(`kotlin-test-junit5`)로 시작한 것을 기능 구현 전에 교체
- **채택**: **Kotest 6.2.5** (`kotest-runner-junit5` · `kotest-assertions-core` · `kotest-extensions-spring`, 우리와 같은 Kotlin 2.2.21로 빌드된 버전). 기본 스타일 **BehaviorSpec**(Given / When / Then 중첩 블록). 스프링 테스트는 모듈마다 `io.kotest.provided.ProjectConfig` 에 `SpringExtension` 을 등록해 `@SpringBootTest` + 테스트 클래스 생성자 주입. JUnit Platform 위에서 돌아 Gradle `useJUnitPlatform()` · CI는 그대로
- **기각**: ① **JUnit 5 + kotlin-test** — 사유: given-when-then을 메서드 이름·주석으로만 표현해 테스트 의도가 코드 구조에 드러나지 않는다. BehaviorSpec은 Given · When · Then 자체가 블록이라 실패 보고서에 `Given > When > Then` 경로가 그대로 찍힌다(전환 시 일부러 틀린 값을 넣어 확인). `shouldBe` 같은 선언형 매처는 `assertEquals(expected, actual)` 인자 순서 혼동이 없다 ② **JUnit 5 + `@Nested` · `@DisplayName`** — 사유: 같은 구조를 흉내 낼 수는 있지만 단계마다 내부 클래스와 어노테이션이 붙어 테스트 하나당 틀 코드가 커진다
- **되돌림**: Kotest와 Spring 연동(컨텍스트 캐시 · 트랜잭션 롤백 · IntelliJ 실행) 문제로 **반나절 이상 막히는 일이 2회** 생기면 그 모듈만 JUnit 5로 되돌린다. 둘 다 JUnit Platform 위라 한 레포에서 섞어 돌릴 수 있다
- **파생**: 팀원 IntelliJ에 Kotest 플러그인 설치 권장 · `spring-boot-starter-test` 에 딸려 오는 JUnit Jupiter는 제외하지 않았다(새 테스트에 JUnit `@Test` 를 쓰지 않는 것은 README 규칙으로) · **미반영**: Notion 「사용할 라이브러리 정리」

